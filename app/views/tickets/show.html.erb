<div id="ticket">
  <header>
    <h1><%= @project.name %></h1>
    <h2><%= @ticket.name %></h2>
    <ul>
      <%= link_to "Edit Ticket", [:edit, @project, @ticket], class: "edit"  %>
    </ul>
  </header>

  <%= simple_format(@ticket.description) %>
</div>