<%= simple_form_for([project, ticket]) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit, class: "btn-primary" %>
  <%= f.input :description %>
  <%= f.input :tag_names, label: "Tags" %>
  <%= render "tag/form", f: f %>
<% end %>
